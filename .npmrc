# Use hoisted node_modules layout instead of pnpm's default symlinked layout.
# REQUIRED on Windows: pnpm's deep .pnpm paths exceed Windows' 250-char
# CMAKE_OBJECT_PATH_MAX, breaking native Android/CMake builds.
# Also recommended by Expo for monorepo setups.
node-linker=hoisted
shamefully-hoist=true
